const { getDefaultConfig } = require('@react-native/metro-config');

const config = getDefaultConfig();

// Si necesitas modificar el comportamiento de Metro, puedes hacerlo aquí.

module.exports = config;
